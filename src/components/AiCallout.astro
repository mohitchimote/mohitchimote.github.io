---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---
<div class="ai-callout">
  <div class="ai-callout-label">Gemma · Local AI</div>
  <div class="ai-callout-title">{title}</div>
  <p>{description}</p>
</div>
